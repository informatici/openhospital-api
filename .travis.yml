language: java
jdk:
 - openjdk8

before_install:
 - git clone -b OP-6-implement-the-new-rest-api https://github.com/informatici/openhospital-core.git
 - cd openhospital-core
 - mvn clean install -DskipTests=true
 - cd -
install: true
script: 
 - mvn clean package -DskipTests=true
